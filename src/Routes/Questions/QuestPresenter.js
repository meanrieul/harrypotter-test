import React from "react";
import styled from "styled-components";
import { Link } from "react-router-dom";

const Container = styled.div`
  padding: 20px;
  display: flex;
  flex-direction: column;
  align-items: center;
`;

const QuestPresenter = () => (
  <Container>
    <div>Q1.</div>
  </Container>
);

export default QuestPresenter;
